package com.concil.edi.commons.enums;

/**
 * Enum representing the origin of a server (internal or external).
 * Maps to VARCHAR2 column des_server_origin in the server table.
 */
public enum ServerOrigin {
    INTERNO,
    EXTERNO
}
