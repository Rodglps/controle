package com.controle.arquivos.common.domain.enums;

/**
 * Enum que representa a origem do valor para identificação de layout.
 */
public enum OrigemValor {
    HEADER,
    TAG,
    FILENAME,
    KEY
}
