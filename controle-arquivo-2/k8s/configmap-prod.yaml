apiVersion: v1
kind: ConfigMap
metadata:
  name: controle-arquivos-config
  labels:
    app: controle-arquivos
    environment: prod
data:
  # RabbitMQ Configuration
  rabbitmq-port: "5671"
  rabbitmq-vhost: "/"
  
  # AWS Configuration
  aws-region: "us-east-1"
  s3-bucket-name: "controle-arquivos-prod"
  
  # Scheduler Configuration
  scheduler-enabled: "true"
  scheduler-cron: "0 */5 * * * *"  # Every 5 minutes
  
  # Application Configuration
  streaming-chunk-size: "10485760"  # 10MB
  streaming-buffer-size: "8192"     # 8KB
  retry-max-attempts: "5"
  retry-backoff-delay: "3000"       # 3 seconds
  processing-timeout-seconds: "300"
  processing-max-reprocessing-attempts: "5"
  
  # Logging Configuration
  log-level-root: "WARN"
  log-level-app: "INFO"
  log-level-spring: "WARN"
  log-level-hibernate: "WARN"
  
  # Database Configuration
  db-hikari-max-pool-size: "50"
  db-hikari-min-idle: "20"
  db-hikari-connection-timeout: "30000"
  
  # RabbitMQ Listener Configuration
  rabbitmq-listener-prefetch: "20"
  rabbitmq-listener-concurrency: "10"
  rabbitmq-listener-max-concurrency: "50"
